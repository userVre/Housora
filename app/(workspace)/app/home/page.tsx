import Image from "next/image";
import Link from "next/link";
import { SiteShell } from "@/components/layout/SiteShell";
import { WorkspaceAccountSync } from "@/components/workspace/WorkspaceAccountSync";
import { UsageSummary } from "@/components/workspace/UsageSummary";
import { toolConfigs } from "@/lib/tool-data";

export default function WorkspaceHomePage() { return <SiteShell bodyClass="page-app-home" workspace><WorkspaceAccountSync /><section className="workspace-home"><div className="workspace-home-heading"><div><span className="workspace-eyebrow">WORKSPACE</span><h1>What will you design today?</h1><p>Choose a tool, upload your space, and turn your idea into a finished design.</p></div><a href="#workspace-tools" className="workspace-primary-action">+ Choose a tool</a></div><div className="workspace-featured-grid"><Link href="/interior-design" className="workspace-featured-card workspace-featured-main"><Image src="/static/images/room-after-640w.webp" alt="Warm modern living room created with Housora" fill sizes="66vw" /><div className="workspace-featured-overlay"><span>MOST POPULAR</span><h2>AI Interior Design</h2><p>Redesign any room while preserving its architecture.</p><strong>Start designing →</strong></div></Link><div className="workspace-featured-stack">{[["/exterior-design", "Exterior Design", "/static/images/hero-after-1376w.avif"], ["/garden-design", "Garden Design", "/static/images/garden-after-320w.webp"]].map(([href, title, image]) => <Link href={href} className="workspace-featured-card workspace-featured-small" key={href}><Image src={image} alt="" fill sizes="33vw" /><div className="workspace-featured-overlay"><h2>{title}</h2><strong>Open tool →</strong></div></Link>)}</div></div><div className="workspace-section-heading" id="workspace-tools"><div><h2>AI Tools</h2><p>Everything you need to reimagine rooms, finishes, and outdoor spaces.</p></div></div><div className="workspace-tools-grid">{Object.entries(toolConfigs).map(([slug, tool]) => <Link href={`/${slug}`} className="workspace-tool-card" key={slug}><div className="workspace-tool-placeholder">{tool.title.replace("AI ", "")[0]}</div><div><h3>{tool.title.replace("AI ", "")}</h3><p>{tool.description}</p></div><span className="workspace-tool-arrow">→</span></Link>)}</div><div className="workspace-home-bottom"><div className="workspace-recent-card"><div className="workspace-section-heading"><div><h2>Recent projects</h2><p>Continue where you left off.</p></div><Link href="/projects">View all</Link></div><div className="workspace-empty-projects"><span>◇</span><div><h3>Your saved projects</h3><p>Open Projects to continue or create a new design.</p></div><Link href="/projects">View projects</Link></div></div><UsageSummary compact /></div></section></SiteShell>; }
